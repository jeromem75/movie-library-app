Movie Library App - v28.5.32
UI v28.5.32 - app-support dry-run and cutover prep

Incremental build after v28.5.31. Adds a grouped read-only App Support dry-run and cutover preparation page/API for a future manual move from legacy app-local runtime data to Application Support. It does not create folders, copy files, move files, delete files, create the marker, edit config, or switch runtime paths.

Safety notes:
- Does not switch live data paths by default.
- Does not move, copy, delete, or modify live database/config/cache files.
- Does not delete, move, restore, edit, or replace the current database/config.
- Does not touch media folders, Tutor files, or the shared Caddyfile.
- Does not add playback, streaming, transcoding, subtitles, or media-client features.
